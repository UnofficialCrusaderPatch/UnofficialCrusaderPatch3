if DEBUG == TRUE then
	--print("menu/_example loaded")
end

local menu = {}

function menu.onShow()
	-- executed every time the menu is loaded
end

function menu.onInit()
	-- executed only once when the menu has been loaded
end

return menu